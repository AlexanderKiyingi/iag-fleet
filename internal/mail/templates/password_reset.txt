Reset your password · {{.AppName}}
===================================

Hi {{if .FullName}}{{.FullName}}{{else}}{{.Username}}{{end}},

Someone — hopefully you — asked to reset the password for the {{.AppName}}
account "{{.Username}}".

Reset here (expires in {{.ExpiryMinutes}} minutes):
  {{.ResetURL}}

If you didn't request this, your password is fine. Ignore the email and the
link will expire.
