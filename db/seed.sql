-- IAG Fleet · seed data
--
-- Domain data is populated via the UI or CSV import. IAM lives in
-- iag-authentication (fleet.* permissions). In-app notification recipients
-- register via GET /api/users/me after platform login.

-- Intentionally empty — keeps cmd/seed idempotent on fresh databases.
